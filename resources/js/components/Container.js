import React, { Component } from 'react';
import styled from 'styled-components';

const Container = styled.div`
    width: 1100px;
    margin: 0 auto;
    background : var(--grey);
`;


export default () => (
    <Container>

    </Container>
)
